kind: Service
apiVersion: v1
metadata:
  name: backend-service
  namespace: three
spec: 
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
