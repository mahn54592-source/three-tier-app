kind: Service
apiVersion: v1
metadata:
  name: frontend-service
  namespace: three
spec: 
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
